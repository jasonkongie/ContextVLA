# ContextVLA context-length sweep — 4 independent Kubernetes Jobs
# Each job trains one context length (4, 8, 12, 16) on 4x A100.
#
# Prerequisites (run once on your local machine):
#   kubectl create secret generic hf-token --from-literal=token=hf_YOUR_TOKEN -n seelab
#   kubectl create secret generic wandb-token --from-literal=token=YOUR_WANDB_KEY -n seelab
#
# Submit all 4 jobs at once:
#   kubectl apply -f jobs/ctx_sweep_jobs.yaml -n seelab
#
# Monitor:
#   kubectl get jobs -n seelab | grep jason-ctx
#   kubectl logs -f job/jason-ctx4-train -n seelab
#   kubectl logs -f job/jason-ctx8-train -n seelab
#   kubectl logs -f job/jason-ctx12-train -n seelab
#   kubectl logs -f job/jason-ctx16-train -n seelab

---
apiVersion: batch/v1
kind: Job
metadata:
  name: jason-ctx4-train
  namespace: seelab
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: ctx-train
    spec:
      restartPolicy: Never
      tolerations:
      - key: nautilus.io/hardware
        operator: Equal
        value: large-gpu
        effect: NoSchedule
      containers:
      - name: ctx-train
        image: ghcr.io/tilmto/hymba:v1
        resources:
          limits:
            nvidia.com/a100: 4
            memory: 256Gi
            cpu: 32
          requests:
            nvidia.com/a100: 4
            memory: 256Gi
            cpu: 32
        env:
        - name: HF_TOKEN
          valueFrom:
            secretKeyRef:
              name: hf-token
              key: token
        - name: WANDB_API_KEY
          valueFrom:
            secretKeyRef:
              name: wandb-token
              key: token
        - name: MUJOCO_GL
          value: osmesa
        - name: TOKENIZERS_PARALLELISM
          value: "false"
        command:
        - bash
        - -c
        - |
          set -euo pipefail
          echo "=== [ctx4] Setting up conda env ==="
          source /workspace/miniforge3/etc/profile.d/conda.sh
          conda create -y -n contextvla python=3.11
          conda activate contextvla

          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
          pip install \
            transformers==4.53.2 safetensors wandb numpy==1.26.4 \
            tqdm tqdm-loggable einops packaging etils beartype \
            jaxtyping tyro pillow requests
          pip install lerobot
          pip install flash-attn --no-build-isolation

          huggingface-cli login --token "$HF_TOKEN" --add-to-git-credential

          echo "=== [ctx4] Starting training ==="
          cd /mymnt/ContextVLA/qwen_fast
          mkdir -p /mymnt/checkpoints/ctx_sweep/ctx4
          torchrun \
            --standalone \
            --nnodes=1 \
            --nproc_per_node=4 \
            --master_port=19001 \
            scripts/train_contextvla.py contextvla_libero_ctx4 \
            --exp-name=ctx4_libero \
            --num-train-steps=10000 \
            --batch-size=16 \
            --accum-steps=2 \
            --checkpoint-base-dir=/mymnt/checkpoints/ctx_sweep/ctx4 \
            --no-resume
          echo "=== [ctx4] Training complete ==="
        volumeMounts:
        - name: jasonkong-volume
          mountPath: /mymnt
        - name: dshm
          mountPath: /dev/shm
      volumes:
      - name: jasonkong-volume
        persistentVolumeClaim:
          claimName: jasonkong-volume
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 32Gi

---
apiVersion: batch/v1
kind: Job
metadata:
  name: jason-ctx8-train
  namespace: seelab
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: ctx-train
    spec:
      restartPolicy: Never
      tolerations:
      - key: nautilus.io/hardware
        operator: Equal
        value: large-gpu
        effect: NoSchedule
      containers:
      - name: ctx-train
        image: ghcr.io/tilmto/hymba:v1
        resources:
          limits:
            nvidia.com/a100: 4
            memory: 256Gi
            cpu: 32
          requests:
            nvidia.com/a100: 4
            memory: 256Gi
            cpu: 32
        env:
        - name: HF_TOKEN
          valueFrom:
            secretKeyRef:
              name: hf-token
              key: token
        - name: WANDB_API_KEY
          valueFrom:
            secretKeyRef:
              name: wandb-token
              key: token
        - name: MUJOCO_GL
          value: osmesa
        - name: TOKENIZERS_PARALLELISM
          value: "false"
        command:
        - bash
        - -c
        - |
          set -euo pipefail
          echo "=== [ctx8] Setting up conda env ==="
          source /workspace/miniforge3/etc/profile.d/conda.sh
          conda create -y -n contextvla python=3.11
          conda activate contextvla

          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
          pip install \
            transformers==4.53.2 safetensors wandb numpy==1.26.4 \
            tqdm tqdm-loggable einops packaging etils beartype \
            jaxtyping tyro pillow requests
          pip install lerobot
          pip install flash-attn --no-build-isolation

          huggingface-cli login --token "$HF_TOKEN" --add-to-git-credential

          echo "=== [ctx8] Starting training ==="
          cd /mymnt/ContextVLA/qwen_fast
          mkdir -p /mymnt/checkpoints/ctx_sweep/ctx8
          torchrun \
            --standalone \
            --nnodes=1 \
            --nproc_per_node=4 \
            --master_port=19001 \
            scripts/train_contextvla.py contextvla_libero_ctx8 \
            --exp-name=ctx8_libero \
            --num-train-steps=10000 \
            --batch-size=16 \
            --accum-steps=2 \
            --checkpoint-base-dir=/mymnt/checkpoints/ctx_sweep/ctx8 \
            --no-resume
          echo "=== [ctx8] Training complete ==="
        volumeMounts:
        - name: jasonkong-volume
          mountPath: /mymnt
        - name: dshm
          mountPath: /dev/shm
      volumes:
      - name: jasonkong-volume
        persistentVolumeClaim:
          claimName: jasonkong-volume
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 32Gi

---
apiVersion: batch/v1
kind: Job
metadata:
  name: jason-ctx12-train
  namespace: seelab
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: ctx-train
    spec:
      restartPolicy: Never
      tolerations:
      - key: nautilus.io/hardware
        operator: Equal
        value: large-gpu
        effect: NoSchedule
      containers:
      - name: ctx-train
        image: ghcr.io/tilmto/hymba:v1
        resources:
          limits:
            nvidia.com/a100: 4
            memory: 256Gi
            cpu: 32
          requests:
            nvidia.com/a100: 4
            memory: 256Gi
            cpu: 32
        env:
        - name: HF_TOKEN
          valueFrom:
            secretKeyRef:
              name: hf-token
              key: token
        - name: WANDB_API_KEY
          valueFrom:
            secretKeyRef:
              name: wandb-token
              key: token
        - name: MUJOCO_GL
          value: osmesa
        - name: TOKENIZERS_PARALLELISM
          value: "false"
        command:
        - bash
        - -c
        - |
          set -euo pipefail
          echo "=== [ctx12] Setting up conda env ==="
          source /workspace/miniforge3/etc/profile.d/conda.sh
          conda create -y -n contextvla python=3.11
          conda activate contextvla

          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
          pip install \
            transformers==4.53.2 safetensors wandb numpy==1.26.4 \
            tqdm tqdm-loggable einops packaging etils beartype \
            jaxtyping tyro pillow requests
          pip install lerobot
          pip install flash-attn --no-build-isolation

          huggingface-cli login --token "$HF_TOKEN" --add-to-git-credential

          echo "=== [ctx12] Starting training ==="
          cd /mymnt/ContextVLA/qwen_fast
          mkdir -p /mymnt/checkpoints/ctx_sweep/ctx12
          torchrun \
            --standalone \
            --nnodes=1 \
            --nproc_per_node=4 \
            --master_port=19001 \
            scripts/train_contextvla.py contextvla_libero_ctx12 \
            --exp-name=ctx12_libero \
            --num-train-steps=10000 \
            --batch-size=16 \
            --accum-steps=2 \
            --checkpoint-base-dir=/mymnt/checkpoints/ctx_sweep/ctx12 \
            --no-resume
          echo "=== [ctx12] Training complete ==="
        volumeMounts:
        - name: jasonkong-volume
          mountPath: /mymnt
        - name: dshm
          mountPath: /dev/shm
      volumes:
      - name: jasonkong-volume
        persistentVolumeClaim:
          claimName: jasonkong-volume
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 32Gi

---
apiVersion: batch/v1
kind: Job
metadata:
  name: jason-ctx16-train
  namespace: seelab
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: ctx-train
    spec:
      restartPolicy: Never
      tolerations:
      - key: nautilus.io/hardware
        operator: Equal
        value: large-gpu
        effect: NoSchedule
      containers:
      - name: ctx-train
        image: ghcr.io/tilmto/hymba:v1
        resources:
          limits:
            nvidia.com/a100: 4
            memory: 256Gi
            cpu: 32
          requests:
            nvidia.com/a100: 4
            memory: 256Gi
            cpu: 32
        env:
        - name: HF_TOKEN
          valueFrom:
            secretKeyRef:
              name: hf-token
              key: token
        - name: WANDB_API_KEY
          valueFrom:
            secretKeyRef:
              name: wandb-token
              key: token
        - name: MUJOCO_GL
          value: osmesa
        - name: TOKENIZERS_PARALLELISM
          value: "false"
        command:
        - bash
        - -c
        - |
          set -euo pipefail
          echo "=== [ctx16] Setting up conda env ==="
          source /workspace/miniforge3/etc/profile.d/conda.sh
          conda create -y -n contextvla python=3.11
          conda activate contextvla

          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
          pip install \
            transformers==4.53.2 safetensors wandb numpy==1.26.4 \
            tqdm tqdm-loggable einops packaging etils beartype \
            jaxtyping tyro pillow requests
          pip install lerobot
          pip install flash-attn --no-build-isolation

          huggingface-cli login --token "$HF_TOKEN" --add-to-git-credential

          echo "=== [ctx16] Starting training ==="
          cd /mymnt/ContextVLA/qwen_fast
          mkdir -p /mymnt/checkpoints/ctx_sweep/ctx16
          torchrun \
            --standalone \
            --nnodes=1 \
            --nproc_per_node=4 \
            --master_port=19001 \
            scripts/train_contextvla.py contextvla_libero_ctx16 \
            --exp-name=ctx16_libero \
            --num-train-steps=10000 \
            --batch-size=16 \
            --accum-steps=2 \
            --checkpoint-base-dir=/mymnt/checkpoints/ctx_sweep/ctx16 \
            --no-resume
          echo "=== [ctx16] Training complete ==="
        volumeMounts:
        - name: jasonkong-volume
          mountPath: /mymnt
        - name: dshm
          mountPath: /dev/shm
      volumes:
      - name: jasonkong-volume
        persistentVolumeClaim:
          claimName: jasonkong-volume
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 32Gi
